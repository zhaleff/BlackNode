#!/usr/bin/env bash
[ "${1:-}" = "toggle" ] && dunstctl set-paused toggle 2>/dev/null && exit 0
if dunstctl is-paused 2>/dev/null | grep -q true; then
    echo '{"text":"󰂛","tooltip":"DND: On","class":"dnd-active"}'
else
    echo '{"text":"󰂚","tooltip":"DND: Off","class":"dnd-inactive"}'
fi

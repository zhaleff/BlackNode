# ─────────────────────────────────────────────
# THEME: Zenith
# Estilo: Minimalista, centrado, márgenes amplios
# Paleta: Catppuccin Mocha + Lavender
# ─────────────────────────────────────────────

# ─── COLORES ──────────────────────────────────
$base     = rgb(30, 30, 46)
$surface  = rgb(40, 40, 56)
$text     = rgb(205, 214, 244)
$subtext  = rgb(166, 173, 200)
$accent   = rgb(180, 142, 238)   # Lavender
$muted    = rgba(205, 214, 244, 0.12)
$dimtext  = rgba(205, 214, 244, 0.55)

# ─── FUENTES ──────────────────────────────────
$font      = JetBrainsMono Nerd Font
$font_bold = JetBrainsMono Nerd Font Bold

# ─── FONDO ────────────────────────────────────
background {
    monitor =
    path = ~/.config/hypr/wallpaper.png
    blur_passes = 3
    blur_size = 8
    noise = 0.0117
    contrast = 0.8916
    brightness = 0.5000
    vibrancy = 0.1696
    vibrancy_darkness = 0.4
}

# ─── GENERAL ──────────────────────────────────
general {
    no_fade_in = false
    grace = 0
    disable_loading_bar = false
    text_trim = true
}

# ─── HORA ─────────────────────────────────────
label {
    monitor =
    text = cmd[update:1000] echo "$(date +"%H:%M")"
    color = $text
    font_size = 96
    font_family = $font_bold
    position = 0, 280
    halign = center
    valign = center
}

# ─── FECHA ────────────────────────────────────
label {
    monitor =
    text = cmd[update:1000] echo "$(date +"%A, %d de %B")"
    color = $subtext
    font_size = 18
    font_family = $font
    position = 0, 210
    halign = center
    valign = center
}

# ─── LÍNEA DECORATIVA ─────────────────────────
shape {
    monitor =
    size = 60, 2
    color = $accent
    rounding = 0
    border_size = 0
    position = 0, 185
    halign = center
    valign = center
}

# ─── AVATAR DE USUARIO ────────────────────────
# Requiere: ~/.config/hypr/avatar.png
image {
    monitor =
    path = ~/.config/hypr/avatar.png
    size = 120
    rounding = -1        # círculo perfecto
    border_size = 3
    border_color = $accent
    rotate = 0
    reload_time = -1
    position = 0, 40
    halign = center
    valign = center
}

# ─── NOMBRE DE USUARIO ────────────────────────
label {
    monitor =
    text =  $USER
    color = $text
    font_size = 20
    font_family = $font_bold
    position = 0, -50
    halign = center
    valign = center
}

# ─── CAMPO DE CONTRASEÑA ──────────────────────
input-field {
    monitor =
    size = 300, 50
    outline_thickness = 2
    outer_color = $accent
    inner_color = $muted
    font_color = $text
    font_size = 16
    font_family = $font
    fade_on_empty = true
    placeholder_text = <span foreground="##88888899"> contraseña</span>
    hide_input = true
    dots_size = 0.3
    dots_spacing = 0.3
    dots_center = true
    rounding = 12
    position = 0, -130
    halign = center
    valign = center
}

# ─── TEXTO DE ESTADO (al escribir) ────────────
label {
    monitor =
    text = $FAIL
    color = rgb(243, 139, 168)   # Rojo catppuccin para errores
    font_size = 13
    font_family = $font
    position = 0, -180
    halign = center
    valign = center
}

# ─── PIE DE PÁGINA ────────────────────────────
label {
    monitor =
    text = Hyprlock · Zenith
    color = $dimtext
    font_size = 11
    font_family = $font
    position = 0, -40
    halign = center
    valign = bottom
}